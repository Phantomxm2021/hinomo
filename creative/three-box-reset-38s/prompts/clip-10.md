# Clip 10 — Open the matching inventory

Duration: 2 seconds

subject_definitions:
- `<Picture 10>` is the exact first frame: the protected Nomo scanner phone is next to the same Box 1 and its one fixed QR label.
- `<Picture 6>` is the exact final frame: the protected Nomo Box 1 inventory screen lists the three matching items.
- `the phone screen` is protected in both supplied states; the transition may use only one scan-confirmation cue and a direct cut between those exact states.

summary:
One restrained scan cue confirms the existing label, followed by a direct cut to the matching Box 1 inventory.

retention_analysis:
This two-second payoff completes the physical-to-digital loop. A direct cut is faster and more credible than asking the model to invent intermediate navigation.

detailed_description:
Start pixel-aligned to `<Picture 10>`. From 0.0 to 0.45 seconds, hold the phone, closed carton, and one QR label still. At 0.45 seconds, brighten the existing orange scanner beam once for no more than six frames and add a subtle single haptic micro-movement of the complete device; do not alter the QR label or create a box around it. At 0.70 seconds, perform one direct hard cut to `<Picture 6>`—no dissolve, wipe, morph, simulated page animation, or intermediate screen. From 0.70 to 2.0 seconds, hold the authentic Box 1 inventory exactly as supplied: `3 items`, `3 total`, HDMI cable, Power adapter, and Tape measure remain sharp and stationary. Preserve phone geometry and screen pixels. Do not add a success message, popup, button, extra item, new page, hand, second label, second device, cursor, or generated typography. Do not scroll or change counts.

overall_soundscape:
One clean muted scan confirmation at 0.45 seconds and one soft transition tick at the direct cut, then quiet room tone. No speech, repeated beep, alarm, or camera shutter.

non_diegetic_music:
None in the generated clip. Add the single continuous campaign music bed during final editing.
