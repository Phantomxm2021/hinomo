# Clip 04 — AI result appears

Duration: 4 seconds

subject_definitions:
- `<Picture 4>` is the sole visual reference: a front-facing silver iPhone 17 Pro Max displaying the authentic Nomo `Analysis result` screen with three recognized rows and one real `Add to list` button.
- `the phone screen` is a protected product layer. Every visible character, icon, row, color, and spacing is fixed to `<Picture 4>`.

summary:
A restrained completion cue resolves into the fully readable AI result list, then the device holds steady for comprehension.

retention_analysis:
The audience gets an immediate payoff from the previous photo: three objects become structured records. Holding the finished screen long enough to read builds trust and avoids feature invention.

detailed_description:
Start pixel-aligned to `<Picture 4>` with the silver device centered on the warm neutral campaign background. From 0.0 to 0.55 seconds, apply a very subtle soft orange luminance pass across the outer background and the existing `Analysis complete` badge only; this is a lighting cue, not a new interface element. By 0.6 seconds, return all pixels to the exact reference. From 0.6 to 4.0 seconds, hold the complete result screen with no scrolling while adding only a two-pixel-equivalent device parallax and an almost imperceptible one-percent camera push. Keep `3 items found`, `HDMI cable`, `Power adapter`, `Tape measure`, the `AI detected` badges, quantities, descriptions, overflow icons, and the real `Add to list` button exactly readable and stationary. Do not create a cursor, finger, second button, new row, dialog, menu, input, loading spinner, keyboard, QR code, notification, or extra phone. Do not modify text, number of items, font, color, device geometry, or background shapes. No morphing or UI redraw.

overall_soundscape:
One restrained, soft digital completion chime at 0.5 seconds over near-silent room tone. No speech, keyboard, repeated notification, or camera sound.

non_diegetic_music:
None in the generated clip. Add the single continuous campaign music bed during final editing.
