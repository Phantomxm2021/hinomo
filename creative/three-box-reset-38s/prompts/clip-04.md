# Clip 04 — Confirm the photo

Duration: 3 seconds

subject_definitions:
- `<Picture 4>` is the exact photo-confirmation anchor: the captured image shows the same packed Box 1; `Retake` is lower left and `Use Photo` is lower right.
- `the phone screen` is protected; its captured carton image and all controls remain exact.

summary:
[keyframe completion] The user confirms the real packing photo by tapping `Use Photo` once.

retention_analysis:
- `<Picture 4>`: fully_preserved - the confirmation photo, `Retake`, and `Use Photo` remain visible and readable.

detailed_description:
Start pixel-aligned to `<Picture 4>`. Hold the same captured carton photo perfectly stable. From 0.70 to 1.35 seconds, one fingertip enters from lower right, touches only `Use Photo`, and exits. `Retake` remains visible, untouched, and unchanged. Add a restrained one-frame press cue using the existing button pixels, then hold the same confirmation state to the end. Do not replace the photo, tap Retake, show a gallery, invent navigation, change the packed items, or generate any UI text.

overall_soundscape:
One quiet confirmation tap over near-silent room tone.

non_diegetic_music:
None in the generated clip; add the continuous campaign bed in editing.
