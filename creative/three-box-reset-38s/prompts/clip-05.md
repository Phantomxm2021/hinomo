# Clip 05 — Add the HDMI cable

Duration: 4 seconds

subject_definitions:
- `<Picture 4>` is the exact first frame: the authentic Nomo AI result list before the HDMI cable is added.
- `<Picture 5>` is the exact final frame: the HDMI row is gone, the two remaining rows have moved upward, and the authentic confirmation reads `HDMI cable added to Box 1`.
- `the phone screen` is protected except for the single defined row-removal transition between the two supplied references.

summary:
One fingertip taps the real `Add to list` button once. Only the HDMI cable row disappears; the remaining rows rise and the supplied confirmation appears.

retention_analysis:
The visible cause-and-effect is the core product proof. Restricting the change to one known row keeps the action understandable and prevents the model from inventing navigation or hidden functionality.

detailed_description:
Begin pixel-aligned to `<Picture 4>`. The silver iPhone, background, device geometry, and every non-changing UI region remain fixed. From 0.35 to 1.20 seconds, one anatomically correct fingertip enters from the lower right and approaches the center of the existing dark `Add to list` button. At 1.25 seconds, make one clean tap: contact, a restrained one-frame button depression using the same button pixels, then immediate release. From 1.45 to 2.25 seconds, the full HDMI cable card contracts vertically and fades as one coherent row; no other card changes content. The Power adapter and Tape measure rows translate upward smoothly to occupy the vacated space. From 2.25 to 2.65 seconds, reveal the supplied confirmation region exactly as it exists in `<Picture 5>`. By 2.7 seconds, the screen must be pixel-aligned to `<Picture 5>` and remain still through 4.0 seconds. The HDMI row does not reappear. Do not create an extra item, button, finger, dialog, keyboard, input, page transition, QR code, or animation outside the defined regions. Preserve all exact text and colors.

overall_soundscape:
One subtle fingertip tap, followed by a short warm confirmation tick. No speech, typing, swipe, repeated chime, or device vibration rattle.

non_diegetic_music:
None in the generated clip. Add the single continuous campaign music bed during final editing.
