# Clip 09 — Bring the scanner to the label

Duration: 4 seconds

subject_definitions:
- `<Picture 9>` is the exact first visual state: the silver iPhone 17 Pro Max displays the authentic Nomo `Scan to view` scanner on the warm neutral background.
- `<Picture 10>` is the exact final visual state: the same phone and protected scanner screen are positioned at left in the warm room while the same closed Box 1 and its one complete horizontal Nomo Box label remain visible at right.
- `the phone screen` and `the complete Nomo Box label` are protected layers. The label's QR area, box identity, storage metadata, instruction, paper edge, placement, and pixels must remain unchanged.

summary:
A fast, clean match move takes the scanner from the product-stage frame into the real room, then the phone steadies toward the QR area inside the same complete Nomo Box label.

retention_analysis:
The purposeful spatial transition reconnects the product interface to the labeled carton without asking the model to fabricate a camera feed. Holding the label and scanner together makes the retrieval action immediately legible.

detailed_description:
Begin pixel-aligned to `<Picture 9>`. From 0.0 to 0.55 seconds, move the complete phone as one rigid object toward camera left while executing a direct match cut—not a dissolve or morph—from the warm neutral background to the supplied room in `<Picture 10>`. At the cut, preserve the exact silver frame, Dynamic Island, bezel, screen pixels, and device scale. From 0.55 to 2.6 seconds, continue a controlled eight-percent forward-and-right settling motion of the phone as if an unseen hand is positioning the scanner toward the QR area printed on the left side of the one complete horizontal Nomo Box label. Keep the device front-facing enough for the authentic scanner UI to remain readable; do not generate a live camera image inside the dark scanner region. The box and the entire fixed information card—including `Nomo Box`, `BX-00038`, storage copy, instruction, and QR—remain stationary and sharp. From 2.6 to 4.0 seconds, hold exactly on `<Picture 10>` with minimal natural micro-motion. Do not crop, redraw, mirror, animate, duplicate, occlude, separate, or move any part of the label. Do not add a hand, second phone, second code, new UI, reticle, text, flash, or button. No background morph after the direct cut.

overall_soundscape:
A restrained cloth-and-device movement sound, quiet room tone after the match cut, and no confirmation beep yet. No speech, shutter, swipe, or electronic voice.

non_diegetic_music:
None in the generated clip. Add the single continuous campaign music bed during final editing.
