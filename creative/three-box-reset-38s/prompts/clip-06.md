# Clip 06 — Add one item

Duration: 3 seconds

subject_definitions:
- `<Picture 5>` is the first frame with three pending items.
- `<Picture 6>` is the final protected AI after-add state: HDMI cable is removed, two items remain pending, and the background-submission notice is visible.

summary:
[reference generation] A single `Add to list` tap removes only HDMI cable from the pending list.

retention_analysis:
- `<Picture 5>`: partially_preserved - the protected list begins with all three candidates; only HDMI cable is submitted.
- `<Picture 6>`: fully_preserved - the two pending candidates and submission notice are fixed at the end.

detailed_description:
Begin on `<Picture 5>`. A fingertip taps the existing HDMI cable `Add to list` button once at 0.85 seconds. The HDMI cable card contracts and disappears as one unit; only White power adapter and Tape measure remain. Reveal `<Picture 6>` with `2 items pending` and `1 item submitted and being added in the background`, then hold. Do not remove another row, alter the two remaining items, make an automatic submission, invent a page transition, search, or review action.

overall_soundscape:
One subtle tap followed by one warm confirmation tick.

non_diegetic_music:
None in the generated clip; add the continuous campaign bed in editing.
