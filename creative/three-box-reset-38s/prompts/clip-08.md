# Clip 08 — Attach one QR label

Duration: 4 seconds

subject_definitions:
- `<Picture 7>` is the exact first frame: the closed Box 1 has no label anywhere.
- `<Picture 8>` is the exact final frame: the same closed Box 1 has one and only one supplied Nomo QR label at the fixed top-right position.
- `the QR label` is a protected graphic layer derived from `<Picture 8>`; its black-and-white modules must not be redesigned, multiplied, mirrored, or replaced.

summary:
One hand introduces the single QR label, aligns it once, presses it onto the closed top, and releases. The code appears for the first time only through this physical action.

retention_analysis:
The label is the bridge between the physical box and later retrieval. Showing one clear attachment prevents the continuity error where a code already exists before another is added.

detailed_description:
Start exactly on `<Picture 7>` and preserve the locked room, camera, carton, closed seam, lighting, and shadows. From 0.20 to 1.10 seconds, one anatomically correct right hand enters from camera right holding a single square white-backed label by two corners. Before the hand enters, no QR pattern or label is visible anywhere. From 1.10 to 2.15 seconds, the hand lowers that one label onto the exact top-right location defined by `<Picture 8>`, keeping the printed face visible and the code orientation stable. From 2.15 to 3.10 seconds, two fingertips press from center toward the edges once, with subtle adhesive contact and no sliding. From 3.10 to 3.55 seconds, the fingertips lift away; the one label remains fixed. End from 3.55 to 4.0 seconds pixel-aligned to `<Picture 8>`. Do not introduce a second label, pre-existing code, replacement code, loose backing paper, tape, text, logo, phone, open flap, or extra hand. Never distort, animate, rewrite, rotate, mirror, or blur the final QR pixels.

overall_soundscape:
Soft paper handling, one quiet adhesive placement sound, and two gentle fingertip presses over the same room tone. No speech, scanner beep, duplicate peel, or exaggerated sticker sound.

non_diegetic_music:
None in the generated clip. Add the single continuous campaign music bed during final editing.
